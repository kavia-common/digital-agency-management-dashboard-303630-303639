# Minimal environment example for project_management_backend
# Copy to .env and fill values as needed.

# Backend server
PORT=3001
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001
SITE_URL=http://localhost:3000

# CORS (comma-separated). If omitted, backend defaults allow localhost dev origins.
# ALLOWED_ORIGINS=http://localhost:3000

# Supabase (optional but required for auth + DB features)
SUPABASE_URL=
SUPABASE_KEY=

# JWT (optional). If not set, local JWT verification is disabled; Supabase token verification may still work.
JWT_SECRET=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
